{
  "name": "chronicles-of-zoal",
  "version": "0.1.0",
  "description": "The Chronicles of Zoal: Ember Awakening — FPS dungeon crawler / hack-and-slash in Three.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "proxy": "npm --prefix server start",
    "proxy:dev": "npm --prefix server run dev"
  },
  "dependencies": {
    "@dimforge/rapier3d-compat": "^0.14.0",
    "howler": "^2.2.4",
    "simplex-noise": "^4.0.3",
    "three": "^0.160.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
