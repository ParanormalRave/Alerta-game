# Client (Vite) env — copy to .env to override defaults.
# Base URL of the 0G proxy (server/). Defaults to http://localhost:8787.
VITE_ZG_PROXY_URL=http://localhost:8787

# Set to 1 to disable all 0G calls (pure offline play).
# VITE_ZG_DISABLE=1
